options:
  connection: globocam_classic
  type: INNODB
  collate: utf8_unicode_ci
  charset: utf8

Translation:
  columns:
    msgid: string(255)
    msgstr: string
    language_id: integer
  indexes:
    unique_pair:
      fields:
        msgid: []
        language_id: []
      type: unique
  relations:
    Language:
      type: one
      onDelete: CASCADE
      onUpdate: CASCADE