options:
  connection: globocam_classic
  type: INNODB
  collate: utf8_unicode_ci
  charset: utf8 

detect_relations: true


Tag:
  columns:
    name: 
      type: string(255)
  indexes:
    name_index:
      fields:
        name: []
      type: unique
