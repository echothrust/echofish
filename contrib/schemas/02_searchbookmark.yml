options:
  connection: globocam_classic
  type: INNODB
  collate: utf8_unicode_ci
  charset: utf8 

detect_relations: true


Searchbookmark:
  columns:
    name: string(255)
    container: string(50)
    module: string(50)
    action: string(50)
    query: string(1024)
    user_id: integer
  relations:
    User:
      onDelete: CASCADE
      onUpdate: CASCADE
  indexes:
    name_index:
      fields:
        name: []
      type: unique

SearchbookmarkTag:
  columns:
    searchbookmark_id: 
      type: integer
      primary: true
    tag_id: 
      type: integer
      primary: true
  relations:
    Searchbookmark:
      onDelete: CASCADE
      onUpdate: CASCADE
    Tag:
      onDelete: CASCADE
      onUpdate: CASCADE